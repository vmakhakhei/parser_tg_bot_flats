# 🚀 Инструкция по деплою

## ✅ Статус: Код запушен в репозиторий

### Информация о коммите
- **Ветка:** `2026-01-18-5h17`
- **Репозиторий:** `https://github.com/vmakhakhei/parser_tg_bot_flats.git`
- **Последний коммит:** Рефакторинг проекта: модульная архитектура, тесты, качество кода

### Создать Pull Request
🔗 https://github.com/vmakhakhei/parser_tg_bot_flats/pull/new/2026-01-18-5h17

## 📦 Что было добавлено

### Новые файлы и директории
- ✅ `bot/` - новая модульная структура бота
  - `bot/handlers/` - обработчики команд
  - `bot/services/` - бизнес-логика
  - `bot/middlewares/` - middleware
- ✅ `tests/` - unit-тесты (90 тестов)
- ✅ `scrapers/dto.py` - Data Transfer Objects
- ✅ `scrapers/http_client.py` - унифицированный HTTP-клиент
- ✅ Документация: `CODE_QUALITY.md`, `TESTING.md`, `FINAL_CHECK.md`
- ✅ Конфигурация: `.flake8`, `pyproject.toml`, `pytest.ini`
- ✅ CI/CD: `.github/workflows/tests.yml`

### Обновленные файлы
- ✅ `README.md` - обновлен с архитектурой и инструкциями
- ✅ `main.py` - рефакторен для новой структуры
- ✅ `scrapers/*.py` - обновлены для использования нового HTTP-клиента
- ✅ `database.py` - улучшена абстракция
- ✅ `requirements.txt` - обновлены зависимости

## 🚀 Деплой на платформы

### Railway.app (Рекомендуется)

1. **Подключите репозиторий:**
   - Зайдите на [railway.app](https://railway.app)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий `parser_tg_bot_flats`
   - Выберите ветку `2026-01-18-5h17` или `main` (после merge)

2. **Настройте переменные окружения:**
   ```
   BOT_TOKEN=ваш_токен_бота
   CHECK_INTERVAL=720
   DEFAULT_CITY=барановичи
   DEFAULT_MIN_ROOMS=1
   DEFAULT_MAX_ROOMS=4
   DEFAULT_MIN_PRICE=0
   DEFAULT_MAX_PRICE=100000
   MAX_PHOTOS=3
   USE_TURSO_CACHE=true (опционально)
   TURSO_DB_URL=ваш_url (если используете Turso)
   TURSO_AUTH_TOKEN=ваш_токен (если используете Turso)
   ```

3. **Настройте команду запуска:**
   - Railway автоматически определит `python main.py` из `Procfile`
   - Или укажите вручную: `python main.py`

4. **Деплой:**
   - Railway автоматически задеплоит при пуше в репозиторий
   - Или нажмите "Deploy" вручную

5. **Проверка:**
   - Проверьте логи в Railway dashboard
   - Отправьте команду `/start` боту в Telegram

### Render.com

1. **Создайте новый Web Service:**
   - Зайдите на [render.com](https://render.com)
   - Нажмите "New +" → "Web Service"
   - Подключите репозиторий `parser_tg_bot_flats`
   - Выберите ветку `2026-01-18-5h17` или `main`

2. **Настройки:**
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Environment:** Python 3
   - **Python Version:** 3.9 или выше (указано в `runtime.txt`)

3. **Переменные окружения:**
   - Добавьте все переменные из `.env` в настройках сервиса
   - См. список выше в разделе Railway

4. **Деплой:**
   - Render автоматически задеплоит при пуше в main/master ветку
   - Или нажмите "Manual Deploy"

### Fly.io

1. **Установите Fly CLI:**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Логин:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   cd "/Users/vmakhakei/TG BOT"
   fly launch
   ```

4. **Настройте переменные:**
   ```bash
   fly secrets set BOT_TOKEN=ваш_токен
   fly secrets set CHECK_INTERVAL=720
   fly secrets set DEFAULT_CITY=барановичи
   # ... остальные переменные
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

## 🔄 Слияние с main веткой

Если хотите смержить изменения в main:

```bash
# Переключитесь на main
git checkout main

# Обновите main
git pull origin main

# Смержите вашу ветку
git merge 2026-01-18-5h17

# Запушьте изменения
git push origin main
```

Или создайте Pull Request через GitHub:
🔗 https://github.com/vmakhakhei/parser_tg_bot_flats/pull/new/2026-01-18-5h17

## ✅ Проверка после деплоя

### 1. Проверьте логи
```bash
# Railway
railway logs

# Render
# Логи доступны в дашборде

# Fly.io
fly logs
```

### 2. Проверьте работу бота
- Отправьте команду `/start` боту в Telegram
- Проверьте, что бот отвечает
- Проверьте команду `/help`
- Проверьте команду `/sources`

### 3. Проверьте тесты (CI/CD)
- GitHub Actions автоматически запустит тесты при пуше
- Проверьте статус в `.github/workflows/tests.yml`
- Все 90 тестов должны пройти успешно

## 🔙 Откат изменений (если нужно)

Если что-то пошло не так, можно откатиться к предыдущей версии:

```bash
# Найдите предыдущий коммит
git log --oneline

# Откатитесь к нужному коммиту
git checkout <commit_hash>

# Запушьте изменения
git push --force origin 2026-01-18-5h17
```

## ⚠️ Важные замечания

### 1. Переменные окружения
- ✅ Убедитесь, что все переменные из `.env` добавлены в настройки платформы
- ✅ Особенно важно: `BOT_TOKEN` (обязательно!)
- ✅ Проверьте, что токен правильный и не истек

### 2. Зависимости
- ✅ Убедитесь, что все зависимости из `requirements.txt` установлены
- ✅ Проверьте версию Python (должна быть 3.9+, указано в `runtime.txt`)

### 3. База данных
- ✅ Если используете Turso, убедитесь, что переменные `TURSO_DB_URL` и `TURSO_AUTH_TOKEN` установлены
- ✅ Если используете SQLite, убедитесь, что есть права на запись
- ✅ Для Turso: запустите `python setup_turso_tables.py` один раз для создания таблиц

### 4. Логи
- ✅ Проверьте логи после деплоя на наличие ошибок
- ✅ Логи также сохраняются в `logs/app.log` (если есть права на запись)

### 5. Тесты
- ✅ Все 90 тестов должны проходить успешно
- ✅ CI/CD автоматически запустит тесты при пуше
- ✅ Проверьте статус в GitHub Actions

## 📊 Статистика проекта

- ✅ **90 тестов** пройдено успешно
- ✅ **37 Python файлов** в проекте
- ✅ **3 основные директории** (bot/, scrapers/, tests/)
- ✅ **0 ошибок flake8** (кроме C901 - сложность функций)
- ✅ **Модульная архитектура** с разделением на слои

## 🆘 Поддержка

Если возникли проблемы:

1. **Проверьте логи платформы:**
   - Railway: Dashboard → Logs
   - Render: Dashboard → Logs
   - Fly.io: `fly logs`

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные установлены
   - Особенно `BOT_TOKEN`

3. **Проверьте зависимости:**
   - Убедитесь, что все зависимости установлены
   - Проверьте версию Python

4. **Проверьте локально:**
   - Убедитесь, что бот запускается локально: `python main.py`
   - Проверьте, что тесты проходят: `pytest tests/ -v`

5. **Проверьте документацию:**
   - `README.md` - основная документация
   - `FINAL_CHECK.md` - результаты финальной проверки
   - `TESTING.md` - руководство по тестированию

## ✨ Что нового в этой версии

- 🏗️ **Модульная архитектура** - разделение на handlers, services, middlewares
- 🧪 **90 unit-тестов** - полное покрытие парсеров и сервисов
- 📝 **Качество кода** - black, flake8, type hints, короткие функции
- 📚 **Документация** - обновлен README с архитектурой и инструкциями
- 🛡️ **Защита от ошибок** - парсеры защищены от падений
- 🔧 **Унифицированный HTTP-клиент** - единый клиент для всех парсеров
- ✅ **DTO для валидации** - валидация данных парсеров

## 🎉 Готово к деплою!

Все изменения запушены в репозиторий. Выберите платформу и следуйте инструкциям выше.
