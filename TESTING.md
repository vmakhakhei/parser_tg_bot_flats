# 🧪 Руководство по тестированию

## Быстрый ответ на вопрос: "Нужен ли отдельный environment?"

**Короткий ответ:** Нет, отдельный environment не обязателен, но рекомендуется для изоляции зависимостей.

**Длинный ответ:**
- ✅ Тесты полностью изолированы и не требуют реальных зависимостей
- ✅ Можно запускать в том же окружении, что и основной код
- ✅ Рекомендуется использовать виртуальное окружение для чистоты
- ✅ Тесты не требуют .env файла или токенов

## 🚀 Запуск тестов

### Вариант 1: Без виртуального окружения (проще всего)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск тестов
python run_tests.py
```

**Преимущества:**
- Быстро и просто
- Не требует настройки окружения
- Подходит для быстрой проверки

**Недостатки:**
- Может конфликтовать с системными пакетами
- Не изолирует зависимости проекта

### Вариант 2: С виртуальным окружением (рекомендуется)

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация (Linux/Mac)
source venv/bin/activate

# Активация (Windows)
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск тестов
python run_tests.py

# Деактивация (когда закончите)
deactivate
```

**Преимущества:**
- Изоляция зависимостей
- Чистое окружение для тестов
- Не влияет на системные пакеты

### Вариант 3: В CI/CD (GitHub Actions, GitLab CI и т.д.)

```yaml
# Пример для GitHub Actions
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Run tests
        run: |
          pytest tests/ -v
```

## 📋 Что тестируется

### ✅ Полностью изолированные тесты

Все тесты используют моки и не требуют реальных зависимостей:

1. **HTTP-запросы** → Мокируются через `AsyncMock`
2. **База данных** → Не используется в тестах парсеров
3. **Переменные окружения** → Не требуются
4. **Токены** → Не требуются

### 📦 Структура тестов

```
tests/
└── scrapers/
    ├── test_dto.py          # Валидация ListingDTO
    ├── test_kufar.py        # KufarScraper с моками
    ├── test_etagi.py        # EtagiScraper с моками
    ├── test_realt.py        # RealtByScraper с моками
    ├── test_domovita.py     # DomovitaScraper с моками
    ├── test_gohome.py       # GoHomeScraper с моками
    └── test_onliner.py      # OnlinerRealtScraper с моками
```

## 🎯 Примеры запуска

### Базовый запуск

```bash
# Все тесты
python run_tests.py

# Или через pytest
pytest tests/
```

### С покрытием кода

```bash
# С HTML отчетом
python run_tests.py --coverage

# Отчет будет в htmlcov/index.html
# Откройте в браузере для просмотра
```

### Выборочный запуск

```bash
# Только тесты Kufar
python run_tests.py tests/scrapers/test_kufar.py

# Тесты с "dto" в названии
python run_tests.py -k dto

# Конкретный тест
pytest tests/scrapers/test_kufar.py::TestKufarScraper::test_fetch_listings_success
```

### Подробный вывод

```bash
# Максимально подробный вывод
pytest tests/ -vv

# С выводом print-ов
pytest tests/ -s

# Остановка на первой ошибке
pytest tests/ -x
```

## 🔍 Что проверяют тесты

### 1. Валидация ListingDTO (`test_dto.py`)
- ✅ Корректность полей (title, price, url, location, source)
- ✅ Обработка ошибок (пустые значения, невалидные данные)
- ✅ Конвертация из словаря
- ✅ Валидация URL

### 2. Парсеры (все `test_*.py`)
- ✅ Мокирование HTTP-запросов
- ✅ Парсинг валидных данных
- ✅ Обработка пустых ответов
- ✅ Обработка ошибок
- ✅ Проверка структуры ListingDTO из парсинга
- ✅ Фильтрация объявлений
- ✅ Пагинация (для Kufar)

## 🛠️ Конфигурация

Проект использует `pytest.ini` для настройки:
- Автоматическое обнаружение тестов
- Настройки асинхронных тестов (`asyncio_mode = auto`)
- Маркеры для категоризации
- Настройки логирования

## ❓ Частые вопросы

### Нужен ли .env файл для тестов?
**Нет.** Тесты полностью изолированы и не читают .env файл.

### Нужны ли токены для тестов?
**Нет.** Все HTTP-запросы мокируются, токены не используются.

### Можно ли запускать тесты без интернета?
**Да.** Все тесты используют моки и не требуют сетевого подключения.

### Нужна ли база данных для тестов?
**Нет.** Тесты парсеров не используют базу данных.

### Как запустить тесты в Docker?
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
RUN pytest tests/ -v
```

### Как интегрировать в CI/CD?
См. пример выше для GitHub Actions. Аналогично для других систем.

## 📊 Покрытие кода

После запуска с `--coverage`:
- Терминальный отчет показывается сразу
- HTML отчет в `htmlcov/index.html`
- Откройте в браузере для детального просмотра

```bash
python run_tests.py --coverage
# Затем откройте htmlcov/index.html
```

## 🐛 Отладка тестов

### Запуск с отладчиком

```bash
# С выводом всех print
pytest tests/ -s

# Остановка на первой ошибке
pytest tests/ -x

# Запуск только упавших тестов
pytest tests/ --lf  # last failed
```

### Просмотр логов

Тесты не пишут в логи по умолчанию. Если нужно логирование:

```bash
pytest tests/ --log-cli-level=INFO
```

## ✅ Best Practices

1. **Используйте виртуальное окружение** для изоляции
2. **Запускайте тесты перед коммитом** (`python run_tests.py`)
3. **Проверяйте покрытие кода** периодически (`--coverage`)
4. **Добавляйте тесты** при добавлении нового функционала
5. **Тесты должны быть быстрыми** (все моки, без реальных запросов)

## 🎉 Готово!

Теперь вы можете запускать тесты в любой среде без дополнительной настройки!
