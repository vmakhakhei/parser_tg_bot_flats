# 🆓 Бесплатный хостинг для Telegram бота

## Лучшие бесплатные варианты

### 1. 🏆 Railway.app (РЕКОМЕНДУЮ)

**Преимущества:**
- 500 часов бесплатно в месяц (хватит на весь месяц если не запускать другие проекты)
- Простой деплой через GitHub
- Поддержка переменных окружения
- Автоматический редеплой при изменениях

**Как использовать:**

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект → Deploy from GitHub repo
3. Загрузите ваш код на GitHub
4. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен бота
5. Railway автоматически обнаружит Python и запустит проект

Создайте файл `Procfile`:
```
worker: python main.py
```

---

### 2. 🎯 Render.com

**Преимущества:**
- 750 часов бесплатно в месяц
- Background Workers для ботов
- Простой интерфейс

**Как использовать:**

1. Зарегистрируйтесь на [render.com](https://render.com)
2. New → Background Worker
3. Подключите GitHub репозиторий
4. Build Command: `pip install -r requirements.txt`
5. Start Command: `python main.py`
6. Добавьте Environment Variables

Создайте файл `render.yaml`:
```yaml
services:
  - type: worker
    name: apartment-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
```

---

### 3. ☁️ Oracle Cloud Free Tier (ЛУЧШИЙ БЕСПЛАТНЫЙ VPS)

**Преимущества:**
- БЕСПЛАТНЫЙ VPS НАВСЕГДА
- 1 GB RAM, 50 GB диск
- Полный контроль над сервером
- Никаких ограничений по времени работы

**Как использовать:**

1. Зарегистрируйтесь на [cloud.oracle.com](https://cloud.oracle.com)
2. Создайте Compute Instance (Always Free eligible)
3. Выберите Ubuntu 22.04
4. Подключитесь по SSH

```bash
# На сервере:
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip python3-venv git -y

# Клонируйте проект
git clone https://github.com/YOUR_USERNAME/apartment-bot.git
cd apartment-bot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создайте .env файл
nano .env
# Добавьте BOT_TOKEN

# Запустите с помощью systemd (см. ниже)
```

**Настройка systemd для автозапуска:**

```bash
sudo nano /etc/systemd/system/apartment-bot.service
```

```ini
[Unit]
Description=Apartment Monitoring Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/apartment-bot
Environment=PATH=/home/ubuntu/apartment-bot/venv/bin
ExecStart=/home/ubuntu/apartment-bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable apartment-bot
sudo systemctl start apartment-bot

# Проверка статуса
sudo systemctl status apartment-bot

# Просмотр логов
sudo journalctl -u apartment-bot -f
```

---

### 4. 🐍 PythonAnywhere

**Преимущества:**
- Бесплатный тариф
- Простая настройка Python
- Scheduled Tasks для периодических запросов

**Ограничения:**
- Нельзя делать исходящие HTTP запросы на бесплатном тарифе (кроме белого списка)
- Это проблема для парсинга! Но можно для бота.

**Как использовать (только для простых ботов):**

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Загрузите файлы через Files
3. Откройте Bash консоль
4. `pip install --user -r requirements.txt`
5. Настройте Scheduled Task для периодического запуска

---

### 5. 🚀 Fly.io

**Преимущества:**
- 3 shared-cpu-1x 256MB VMs бесплатно
- 3 GB storage

**Как использовать:**

1. Установите flyctl: `curl -L https://fly.io/install.sh | sh`
2. `fly auth signup`
3. В папке проекта: `fly launch`
4. `fly secrets set BOT_TOKEN=xxx`
5. `fly deploy`

Создайте `fly.toml`:
```toml
app = "apartment-bot"
primary_region = "ams"

[build]
  builder = "paketobuildpacks/builder:base"

[env]
  PORT = "8080"

[[services]]
  internal_port = 8080
  protocol = "tcp"
```

---

### 6. 🔄 Replit (с ограничениями)

**Преимущества:**
- Очень простой интерфейс
- Бесплатно

**Ограничения:**
- Бот "засыпает" если не используется
- Нужен UptimeRobot для постоянной работы

---

## 📋 Сравнительная таблица

| Сервис | Время работы | Сложность | Ограничения |
|--------|-------------|-----------|-------------|
| Railway | 500ч/мес | ⭐ Легко | Лимит часов |
| Render | 750ч/мес | ⭐ Легко | Спит после 15 мин неактивности |
| Oracle Cloud | ♾️ Безлимит | ⭐⭐⭐ Средне | Нужно настроить VPS |
| Fly.io | 3 VMs | ⭐⭐ Средне | 256MB RAM |
| PythonAnywhere | ♾️ | ⭐ Легко | Нет внешних запросов |
| Replit | Засыпает | ⭐ Легко | Требует костыли |

---

## 🎯 Моя рекомендация

### Для простого старта: **Railway.app**
- Быстро и просто
- Хватит на месяц непрерывной работы

### Для бесплатного VPS навсегда: **Oracle Cloud**
- Требует настройки, но работает постоянно
- Никаких лимитов и ограничений
- Полный контроль над сервером

---

## 📁 Файлы для деплоя

### Procfile (для Railway/Render)
```
worker: python main.py
```

### runtime.txt
```
python-3.11.0
```

### .dockerignore
```
__pycache__
*.pyc
*.db
.env
.git
*.log
venv
```

---

## ⚠️ Важные советы

1. **Никогда не коммитьте .env файл в Git!** Добавьте его в `.gitignore`

2. **Используйте переменные окружения** на хостинге вместо .env файла

3. **Настройте алерты** чтобы знать если бот упал

4. **Логи** - храните логи для отладки:
   ```python
   logging.basicConfig(
       level=logging.INFO,
       format='%(asctime)s - %(levelname)s - %(message)s'
   )
   ```

5. **Graceful shutdown** - бот корректно завершается при остановке

---

## 🆘 Если нужна помощь

- Railway docs: https://docs.railway.app
- Render docs: https://render.com/docs
- Oracle Cloud docs: https://docs.oracle.com/en-us/iaas/Content/home.htm

